{
  "name": "chatgpt-firefox-exporter",
  "version": "1.0.0",
  "description": "Firefox extension to export ChatGPT conversations to Markdown",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "echo 'Build script not implemented yet'"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
